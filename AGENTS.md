<!-- 手動管理（正準/SSOT）。Claude Code・Codex 等すべての AI コーディングツールが読む共通コンテキスト。独立 Git リポジトリなので親フォルダの AGENTS.md は継承されない（このファイル単体で完結させる）。Claude Code は CLAUDE.md の @AGENTS.md で取り込む。 -->

# 02_web-new-coolance Repository Guidelines

<!-- BEGIN:dev-common v1 -->
## 開発フォルダ共通規約（dev-common v1）

> 正本は開発ルート `AGENTS.md` のこのブロック。各リポジトリへは `tools/sync-dev-common.ps1` で同期する。**ここは手で直さない**（直すなら正本→同期）。

- **コア原則**: シンプル第一（変更は最小限に）／根本原因を解決する（応急処置はしない）／必要な箇所だけ変更し新たなバグを生まない。
- **計画の提示**: 3ステップ以上、または設計判断を含む変更は、着手前に計画を提示して合意を得る。問題が起きたら無理に進めず計画を立て直す。
- **完了前の検証**: 動作確認なしに完了扱いにしない。差分・テスト・ログで正しさを示す。
- **参照対象外**: `#archive/` `_archive/` `archive/` `*_旧*` `*旧版*` は参照・検索・読み取り・編集・実行を含むあらゆる操作の対象外（旧実装の復活・誤った前例の踏襲を防ぐ）。`node_modules/` も前例・検索の対象にしない。例外は、ユーザーが特定ファイルを明示指定した場合と、本ファイル内のブロックが明示する公式ドキュメント参照（Next.js の `node_modules/next/dist/docs/` 等）のみ。
- **コーディングスタイル**: インデント2スペース（HTML/CSS/JS/TS共通）／ファイル名 `kebab-case`／変数・関数名 `camelCase`／クラス名 `PascalCase`。
- **タスク管理**: `tasks/` があるリポジトリでは計画・進捗を `tasks/todo.md`、修正から得た教訓を `tasks/lessons.md` に記録する。無いリポジトリでは作らない。
- **起動場所**: AI ツールは必ず `.git` のあるプロジェクトフォルダを cwd にして起動する。親フォルダの規約は Codex には届かない（入れ子 Git の境界で探索が止まる）。開発ルートでの起動は Claude Code のみ可。
- **スキル**: 自作スキルの正本は skills-hub。リポジトリ内の `.claude/skills` `.agents/skills` にハブ配布物を置かない（第三者スキルは可）。Claude Code 専用の機能（AskUserQuestion・plan モード等）を前提にした指示を他ツールで読んだら「質問は平文で・計画は本文で示す」に読み替える。
<!-- verify-token: DEVCOMMON-2609 -->
<!-- END:dev-common -->

## このリポジトリ固有

<!-- verify-token: REPO-02 -->

- **目的**: 合同会社 Coolance の会社 Web サイト（1ページ構成のコーポレートサイト）。
- **技術**: 素の HTML / CSS / JavaScript（`index.html`・`styles.css`・`script.js`）。ビルド工程・パッケージ管理なし。`index.html` をブラウザで直接開いて確認する。
- **ブランチ**: 作業は `develop`、公開反映は `main`（既定ブランチ）へのマージ。`main` へ直接コミットしない。
- **公開リポジトリ**: GitHub 上で公開されている。社内情報・案件名・個人の連絡先・ローカルパス・認証情報を、コード・コメント・コミットメッセージ・この AGENTS.md に書かない。
- **画像**: ロゴ・QR コード等の画像ファイルは差し替え時に同名で上書きする（HTML 内の参照を増やさない）。
